[project]
name = "nft-deploy"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "colorlog>=6.12.0",
    "concurrent-log-handler>=0.9.29",
    "psutil>=7.2.2",
    "pyroute2>=0.9.6",
]
